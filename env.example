# LexiCare Environment Configuration (.env)

# Server configuration
PORT=8006
HOST=0.0.0.0

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3100

# Database configuration
DATABASE_URL=sqlite:///backend/db/lexicare.db
# For PostgreSQL (production): 
# DATABASE_URL=postgresql://user:password@localhost/lexicare

# API Security
# IMPORTANTE: Cambia questa chiave in produzione! Usata per l'autenticazione API degli EHR
API_KEY=lexicare-development-key

# EHR Integration
# Configura questi domini per consentire chiamate da sistemi EHR esterni
EHR_DOMAIN_1=https://your-ehr-domain.com
# EHR_DOMAIN_2=https://another-ehr-domain.com

# AI Engine Configuration
OLLAMA_BASE_URL=http://localhost:11434
AI_MODEL=medgemma

# OCR Configuration
# Imposta su 'True' per abilitare OCR per PDF basati su immagine
ENABLE_OCR=True
